<h2>単語検索</h2>

<%= form_with url: search_words_path, method: :get, local: true do |form| %>
  <%= form.text_field :q, placeholder: "単語を検索" %>
  <%= form.submit "検索" %>
<% end %>

<hr>

<h3>検索結果</h3>
<% if @words.present? %>
  <ul>
    <% @words.each do |word| %>
      <li><%= word.explanation %></li>
    <% end %>
  </ul>
<% else %>
  <p>該当する単語はありませんでした。</p>
<% end %>
